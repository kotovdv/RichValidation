package com.jquartz.rich.validation.core.verification.expression;

import com.tngtech.java.junit.dataprovider.DataProviderRunner;
import org.junit.runner.RunWith;

/**
 * @author Dmitriy Kotov
 */
@RunWith(DataProviderRunner.class)
public class LogicExpressionTest {
/*
    private static final Integer STUB_SOURCE = 1;

    @DataProvider
    public static Object[][] scenarios() {
        return new Object[][]{
                {new ComparisonExpression<>(literal(10), EQUAL_TO, literal(15)), false},
                {new ComparisonExpression<>(literal(10), NOT_EQUAL_TO, literal(15)), true},
                {new ComparisonExpression<>(literal(10), GREATER_THAN, literal(15)), false},
                {new ComparisonExpression<>(literal(10), LESS_THAN, literal(15)), true},
        };
    }

    @Test
    @UseDataProvider("scenarios")
    public void testLogicExpressionEvaluation(ComparisonExpression<Integer> expression, boolean expectedResult) throws Exception {
        assertThat(expression.apply(STUB_SOURCE)).isEqualTo(expectedResult);
    }*/
}